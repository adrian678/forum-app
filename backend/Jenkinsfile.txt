pipeline {
    agent {
        docker { image 'maven:3.6.3-jdk-8'}
    }
    stages {
        stage('backend'){
            dir("backend"){
                agent{dockerfile true}
                stage('Build') {
                    sh 'echo bananas'
            
                }
            }
        }
        
    }
}