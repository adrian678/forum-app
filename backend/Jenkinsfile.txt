pipeline {
    agent any
    stages {
        stage('backend'){
            agent {
                dockerfile{
                    filename 'backend.Dockerfile'
                    dir 'backend'
                    args '-v /tmp:/tmp'
                }
            }
            steps{
                bat 'cd'
            }
            
        }
        
    }
}