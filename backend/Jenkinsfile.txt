pipeline {
    agent any
    stages {
        stage('backend'){
            agent {
                dockerfile{
                    dir 'backend'
                    filename 'Dockerfile.build'
                    args '-v /tmp:/tmp'
                }
            }
            steps{
                bat 'cd'
            }
            
        }
        
    }
}