pipeline {
    stages {
        stage('backend'){
            steps{
                dir("backend"){
                    agent{dockerfile true}
                    stage('Build') {
                        sh 'echo bananas'
                        echo '${pwd}'
                    }
                }
            }
            
        }
        stage('frontend'){
            echo '${pwd}'
        }
    }
}