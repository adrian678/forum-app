pipeline {
    agent any
    stages {
        stage('backend'){
            agent {
                dockerfile{
                    dir 'backend'
                    label 'backend-img'
                    args '-v /tmp:/tmp'
                }
            }
            steps{
                bat 'cd'
            }
            
        }
        
    }
}